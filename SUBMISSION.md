# Chrome Web Store 제출 프로세스

새 세션에서 이어서 진행할 수 있도록, 현재까지 상태와 남은 작업을 정리한 문서.

## 현재 상태 (완료)

기능 구현 및 코드 감사(기능/UI/성능/보안)까지 끝났고, `main` 브랜치에 초기 커밋 완료.

- MV3 + `declarativeNetRequest` 기반 헤더 수정 (webRequest blocking, 원격 코드, 텔레메트리 없음)
- 팝업 하나로 전체 관리: 행별 on/off, 드래그 순서 변경, solo(이 규칙만 켜기), 전체 선택/해제
- 행별 메모, URL 패턴/정규식 범위 지정 (상세 패널에 접힘)
- 활성 규칙 수를 툴바 아이콘 배지로 표시
- 팝업 내 언어 전환 (English 기본 / 한국어), `manifest.json`도 `_locales`로 로컬라이즈
- `storage.local`만 사용 (sync 아님 — 계정 토큰 값이 구글 계정으로 안 올라감)
- 코드 감사에서 나온 이슈(레이스 컨디션, 빈 URL 패턴이 전체 URL로 새는 문제, 잦은 전체 리렌더로 인한 포커스 유실 등) 전부 수정 완료

파일 구조: `manifest.json`, `background.js`, `common/`(ruleModel.js, i18n.js), `popup/`(popup.html/css/js), `icons/`, `_locales/en`, `_locales/ko`

## 남은 작업 (제출 전)

### 1. 개발자 계정
- [ ] Chrome Web Store Developer Dashboard 가입 여부 확인
- [ ] 미가입 시 1회 $5 등록비 결제 (https://chrome.google.com/webstore/devconsole)

### 2. 프라이버시 정책
`host_permissions: ["<all_urls>"]`를 쓰기 때문에 프라이버시 정책 URL이 요구될 가능성이 높음.
- [ ] "이 확장 프로그램은 사용자 데이터를 수집하거나 외부로 전송하지 않으며, 모든 설정은 브라우저 로컬 저장소에만 저장된다"는 내용의 정책 페이지 작성
- [ ] 호스팅 (GitHub Pages / Gist raw / 간단한 정적 페이지 중 택1)
- [ ] Developer Dashboard의 "Privacy practices" 탭에 URL 등록 + 데이터 사용 설문 응답 (수집 데이터 없음으로 체크)

### 3. 스토어 리스팅 자료
- [ ] 스크린샷 최소 1장, 권장 1280x800 (실제 계정/토큰 값이 아닌 예시 데이터로 캡처할 것)
- [ ] 짧은 설명 / 자세한 설명 문구 (대시보드에 직접 입력, `manifest.json`의 description과 별개)
- [ ] 카테고리 선택 (예: Developer Tools)
- [ ] 프로모션 타일 이미지는 선택 사항, 생략 가능

### 4. 단일 목적(Single Purpose) 설명
- [ ] "HTTP 요청/응답 헤더를 사용자 정의 규칙에 따라 수정한다"는 한 줄 목적 설명 준비 (심사 시 입력)

### 5. 권한 사용 근거 (심사에서 자주 걸리는 부분)
대시보드에 각 권한별로 왜 필요한지 짧게 설명해야 함:
- `declarativeNetRequest`: 헤더 수정 기능의 핵심 API
- `storage`: 사용자가 만든 규칙 저장
- `host_permissions: <all_urls>`: 사용자가 임의의 사이트에 규칙을 적용할 수 있어야 하므로 필요 — 광범위한 호스트 권한은 심사에서 가장 흔한 반려 사유이므로 이 설명을 신경 써서 작성할 것

### 6. 패키징
- [ ] `.git` 등 제외하고 필요한 파일만 zip으로 압축 (manifest.json, background.js, common/, popup/, icons/, _locales/)
- [ ] `manifest.json`의 `version`은 업데이트할 때마다 올려야 함 (현재 0.1.0)

### 7. QA 체크리스트 (제출 전 마지막 확인)
- [ ] 요청 헤더 추가/수정 동작
- [ ] 응답 헤더 추가/수정 동작
- [ ] URL 패턴 / 정규식 필터링 동작
- [ ] 드래그로 순서 변경 후 우선순위(충돌 시 위쪽 규칙 우선) 확인
- [ ] solo / 전체 선택 / 전체 해제 동작
- [ ] 언어 전환(EN/KO) 후 전체 텍스트 반영 확인
- [ ] 배지 숫자가 활성 규칙 수와 일치하는지
- [ ] 브라우저 재시작 후에도 설정이 유지되는지

### 8. 제출 및 심사
- [ ] 대시보드에서 zip 업로드 → 리스팅 정보 + 프라이버시 항목 작성 → 제출
- 심사 기간: 보통 몇 시간~영업일 며칠. 권한 범위가 넓을수록(`<all_urls>`) 더 걸릴 수 있음
- 흔한 반려 사유: 권한 근거 불충분, 프라이버시 정책 누락, 이름이 너무 일반적이거나 상표권 문제, 스크린샷/설명 불일치

## 열린 결정 사항 (사용자가 정할 것)
- **확장 프로그램 이름**: 현재 "Modify Header"는 매우 일반적인 이름 — 스토어 등록 전에 더 구체적인 이름으로 바꿀지 결정 필요
- **프라이버시 정책 호스팅 위치**: 어디에 올릴지 (GitHub Pages 등)
- **스토어 카테고리** 및 리스팅 문구 톤

## 참고 (선택 사항, 이번 목표엔 불필요)
- Edge Add-ons 스토어는 별도 제출 프로세스 (Partner Center, 별도 심사) — Chrome 심사만 목표라면 무시해도 됨. 개인 사용은 압축해제된 확장 로드로 계속 가능.
